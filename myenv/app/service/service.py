# import tensorflow as tf
from flask import Blueprint, request, jsonify

def predict_emotion(input_data):
    return jsonify("Emotion prediction is under construction")

def predict_fallacy(input_data):
    return jsonify("Fallacy prediction is under construction")